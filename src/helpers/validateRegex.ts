
/** 
 * Determines whether a given input string is considered valid based on 
 * the provided regex.
 * @author Austen Money
 */
const validateRegex = (input: string, regex: string): boolean => {


};

export default validateRegex;